name: Daily Discord Post

on:
  schedule:
    - cron: '*/1 * * * *'  # TEST MODE: every 2 minutes
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

     - name: Discord Notification
  uses: tenzin1308/discord-notification@v1.0.0
